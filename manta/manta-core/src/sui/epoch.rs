// Epoch tracking logic is reserved for future functionality
// such as committee change notifications and epoch transition handling.
// In the MVP, epoch checking is handled by the Walrus L3 client
// which uses its own SuiReadClient for committee data.